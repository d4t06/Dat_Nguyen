"use client";

export default function Email() {
	return <input onFocus={(e) => e.target.select()} readOnly className="outline-none bg-transparent" onClick={(e) => {}} value={"datnh0708@gmail.com"} />;
}
